# Source Instructions

This repository was created from the following human instruction:

> I want you to apply this methodology to create a new implementation of the classic game "breakout". I want you to put it in a separate repository that is public in the la3lma github account, check it out into the ~/git/ directory as a subdirectory. The game should be a javascript game, it should have a highscore mechanism for a single user in a single browser, but no multiuser / multi-score. I want you to apply the technology described in this document to that.
>
> In addition I want you to keep a development notebook, in the new repository, as well as the document stack. Use a mermaid diagram that links first to the github issues and then to the github PRs for each of the issues.
>
> I want evidence of the various stages of the build in the form of a webpage, static webpages, in the new repository, where there are screenshots of the game as it evolves (static), but also mp4 movies of the game being played. Use playwright to capture these things. No need to play games to completion, just enough so that I can see that it works in real time.
>
> Use the description I give here as the instructions for how to make the game. I want that logged in the new repo, I also want this example to be documented using the full stack including the notebook, and included as an example of how the method can be used.

Additional human instruction:

> Include a "lab notebook" in the doc stack. It is notes made by the agent, and by the human, and by the human through the agent, during the work, so that decisions and explorations are documented, even if they go a bit around the strict plan outlined in the planning and evidence stack.

Additional human instruction:

> The plan document should be open in the system browser as a tab, and AppleScript should be used to update the tab. The updating should happen through the Makefile.

Additional human instruction:

> Extend the plan to generate a documentation site on GitHub Pages. The documentation pages should be published there, including the plan, TL;DR, notes, and other document-stack pages. The graph legend should be more discrete, with small color buttons rather than a large legend block.

Additional human instruction:

> The GitHub Pages site should look like a polished presentation of what the project is supposed to do, with TL;DR, vision document, PRD, plan, notes, a full browsable evidence set, and the game playable. Add this as the final use case in the plan and update the other documents accordingly.

Additional human instruction:

> Record these changes in the notes file, keep the final publication pass as UC-06, make the repository README point to the Pages site and include a game screenshot, add a pointer to this repository from the original paper manuscript, and add a Makefile target to publish to GitHub Pages.
